/* SQL Server configuration*/

var configSqlServer = {
    user: 'USER',
    password: 'PASSWORD',
    server: 'HOST', 
    database: 'DATABASENAME',
    stream: true,
}

/* Mysql configuration*/

var configMysql = 'mysql://USER:PASSWORD@HOST:PORT/DATABASENAME';